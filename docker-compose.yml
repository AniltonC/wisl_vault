version: '3.8'

services:
  backend:
    build: ./backend
    environment:
      - UPLOAD_DIR=/data
    volumes:
      - ./uploads:/data
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped
